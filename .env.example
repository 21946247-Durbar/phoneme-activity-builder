# Example environment file — copy to `.env` and adjust as needed.
# See prisma.config.ts for how this file is loaded.

# SQLite database (relative to prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# Optional runtime
# NODE_ENV="development"